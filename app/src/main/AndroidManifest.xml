<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="arami265.github.io.flashcards">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".addClass" />
        <activity android:name=".displayClasses" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".addDeck" />
        <activity android:name=".displayDecks" />
        <activity android:name=".AddUser" />
        <activity android:name=".UserLogin" />
        <activity android:name=".deckMenu" />
        <activity android:name=".displayCards" />
        <activity android:name=".addCards" />
        <activity android:name=".displayQuiz" />
        <activity android:name=".displayClassInfo"></activity>

        <meta-data
            tools:replace="android:value"
            android:name="android.support.VERSION"
            android:value="25.4.0" />
    </application>

</manifest>